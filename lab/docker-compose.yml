services:
  pkcs11-lab:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace
      - softhsm-tokens:/var/lib/softhsm/tokens
    working_dir: /workspace
    tty: true
    stdin_open: true

volumes:
  softhsm-tokens:
